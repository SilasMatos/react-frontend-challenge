// API pública da feature `bookshelf`.
// Importe SEMPRE por `@/features/bookshelf` — caminhos internos são bloqueados pelo
// `no-restricted-imports` (eslint.config.js). Dentro da feature, use imports relativos.
//
// Reexporte aqui só o que `routes/` e outras camadas consomem, ex.:
//   export { BookshelfTable } from './components/bookshelf-table'
//   export { useBookshelf } from './hooks/use-bookshelf'
//   export type { BookshelfItem, BookStatus } from './types/bookshelf.types'
export {}
